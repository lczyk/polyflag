[package]
name = "polyflag"
version = "0.0.0"
description = "Repeatable comma-separated set-style cli flags with `-` prefix removal."
readme = "README.md"
edition = "2024"
license = "MIT"
rust-version = "1.90"

[dependencies]
